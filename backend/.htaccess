Options -Indexes
RewriteEngine On
RewriteBase /exclusivegrade/backend/

# Route API requests through the router — CORS is handled in PHP
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/(.*)$ api/index.php?route=$1 [QSA,L]
